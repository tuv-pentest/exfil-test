[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tuvpentest-exfil"
version = "0.1.0"
description = "dummy test project"
readme = "README.md"
requires-python = ">=3.8"